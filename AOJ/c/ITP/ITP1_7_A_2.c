#include <stdio.h>
int main(void){
    int m, f, r;
    while (scanf("%d %d %d", &m, &f, &r), m + f + r != -3) {
        swtich (m + f) {
            case:
        }

    }

    return 0;
}